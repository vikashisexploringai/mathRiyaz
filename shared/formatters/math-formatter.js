const MathFormatter = {
    formatQuestion: (text) => {
        return text;
    },
    formatOptions: (options) => {
        return options;
    },
    formatOption: (text) => {
        return text;
    },
    formatAnswer: (text) => {
        return text;
    }
};

export default MathFormatter;
